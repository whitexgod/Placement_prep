#include<stdio.h>

main()

{

     int l,b,p,a;

     printf("Enter the length of the rectangle =\n ");

     scanf("%d", &l);

     printf("Enter the bredth of the rectangle =\n ");

     scanf("%d", &b);

     p=(2*(l+b));

     a=(l*b);

     printf ("the perimeter of the rectangle = %d \n The area of the rectangle = %d " ,p,a);

}
